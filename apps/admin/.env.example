# Bu dosyayı .env.local olarak kopyalayın.
# DİKKAT: NEXT_PUBLIC_* değişkenler BUILD ANINDA gömülür — prod build'i
# doğru değerle alın (boşsa http://localhost:3001/api/v1 varsayılır).
NEXT_PUBLIC_API_BASE="http://localhost:3001/api/v1"
